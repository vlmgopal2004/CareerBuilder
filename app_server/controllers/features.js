module.exports.getFeaturesPage = (req, res) => {
    // Implement logic to render the Features page (e.g., render a Pug template)
    res.render('features'); // Replace 'features.pug' with your actual Pug template
  };
  