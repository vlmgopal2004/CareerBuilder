module.exports.getLogin = (req, res) => {
    // Implement logic to render the Features page (e.g., render a Pug template)
    res.render('login'); // Replace 'features.pug' with your actual Pug template
  };

  module.exports.getRegistration = (req, res) => {
    // Implement logic to render the Features page (e.g., render a Pug template)
    res.render('signup'); // Replace 'features.pug' with your actual Pug template
  };